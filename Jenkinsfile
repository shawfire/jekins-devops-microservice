# Declarative
pipeline {
	agent any
	stages {
		stage('Build') {
			setps {
				echo "Build"
			}
		}
		stage('Test') {
			setps {
				echo "Test"
			}
		}
		stage('Integration Test') {
			setps {
				echo "Integration Test"
			}
		}		
	}
}
